# Validation

This layer contains business validation and release validation records.

Use this directory for future `BVR-*` artifacts together with review and release documents that provide validation evidence.
