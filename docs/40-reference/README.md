# Reference Documentation

This layer contains reusable implementation references, guides, dictionaries, and supporting material.

Future `RI-*` artifacts belong here together with other reference documents that explain how approved architecture is realized in practice.
